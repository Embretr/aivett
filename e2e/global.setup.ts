import { test as setup } from "@playwright/test"

// Global setup — can be used to seed test data or authenticate
setup("global setup", async () => {
  // No-op for now; individual tests handle their own state
})
